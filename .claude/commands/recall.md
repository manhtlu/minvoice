Recall the current implementation state and provide a summary to the user.

## Steps

1. Read `memories/implementation-state.md`.
2. Present a concise summary to the user including:
   - Current phase (Planning / Development / Testing / etc.)
   - What has been completed.
   - What is currently in progress.
   - What is next (top 2-3 items from "Not Started").
   - Any known issues or blockers.
   - Key decisions made so far.
3. If the file seems outdated (e.g., source files exist but state says "Pre-development"), warn the user and suggest running `/save` first.

## Rules

- Do NOT read source code files — rely only on the state file.
- Keep the summary short — aim for a quick status update, not a full report.
- Respond in Vietnamese.
